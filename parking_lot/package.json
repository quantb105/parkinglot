{
  "name": "parking_lot",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "deploy": "npm install && tsc -b ",
    "copyfile": "cp -R file_inputs.txt build/",
    "build": "cd build && node index.js",
    "start": "npm run deploy && npm run copyfile && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Quan Do",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.6",
    "tslint": "^6.1.2",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "express": "^4.17.1",
    "fs": "0.0.1-security",
    "path": "^0.12.7"
  }
}
